Git commands
git init
git remote add origin "https://github.com/bollinasuresh/Testng.git"
git status
git add -A
git status

git config --global user.name "bollinasuresh"
git config --global user.email "bollinasuresh@gmail.com"

git status
git commit -m "comment"


git push -u origin master
